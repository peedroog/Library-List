import { BrowserRouter as Routes, Route, Switch, Link } from 'react-router-dom';

function Login(){
    return(
        <div className="login">
            <button>LOGIN</button>
            <h1>HOLA</h1>
        </div>
    )
}

export default Login;